{
  "name": "query_selector",
  "version": "0.0.1",
  "description": "A simple API which takes a URL and a querySelector and returns the text for that matched element",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.3",
    "form-data": "^2.3.2",
    "jsdom": "^11.6.2",
    "querystring": "^0.2.0"
  }
}
